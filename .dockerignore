# Keep the build context small and never ship local state into the image.
.git
node_modules
# Built fresh in the build stage; never carry a host copy into the context.
public/dist
.planning
docs
test
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite-*
.env
.env.*
.dev.vars
.DS_Store
*.log
